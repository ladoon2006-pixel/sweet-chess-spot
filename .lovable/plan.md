## بخش ۱: حذف Pi Network
- حذف کامل `src/components/PiAuth.tsx`
- حذف `src/lib/pi.functions.ts`
- حذف دکمه پرداخت ۰.۰۱ Pi از `src/routes/index.tsx`
- حذف اسکریپت Pi SDK از `src/routes/__root.tsx`

## بخش ۲: احراز هویت جدید (ایمیل + گوگل)
- بازنویسی صفحه `/auth` با دو تب:
  - **ایمیل/رمز عبور**: ثبت‌نام با کد تأیید ۶ رقمی (Supabase OTP)، ورود با رمز
  - **ورود با گوگل**: از طریق `lovable.auth.signInWithOAuth("google")`
- فعال‌سازی Google OAuth و قالب‌های ایمیل تأیید
- آپدیت `src/routes/index.tsx` برای نمایش وضعیت ورود/خروج

## بخش ۳: انتخاب زمان بازی آنلاین
- آپدیت `src/routes/play.online.tsx`: قبل از سرچ، انتخاب زمان:
  - بدون زمان / ۵ دقیقه / ۱۰ دقیقه / ۲۰ دقیقه
- اضافه کردن ستون `time_control` به `matchmaking_queue` و `games`
- آپدیت تابع `find_or_join_match` که فقط بازیکن‌های با تایم یکسان رو match کنه
- نمایش تایمر هر بازیکن در صفحه بازی

## بخش ۴: محدودیت ۵ بازی رایگان در روز
- ستون `last_free_reset_date` در `profiles`
- آپدیت `consume_online_game`: اگر تاریخ عوض شده، شمارنده ریست شه و سقف رایگان ۵ بشه (به‌جای ۱۰ فعلی)

## بخش ۵: آپلود عکس پروفایل + گزارش
- ایجاد storage bucket به نام `avatars`
- آپلود از صفحه پروفایل با Crop و حداکثر ۲MB
- دکمه گزارش روی پروفایل کاربران دیگه

## بخش ۶: فیلتر فحش فارسی + گزارش چت
- فایل `src/lib/profanityFilter.ts` با لیست فحش‌های فارسی (کس، کیر، کون، جنده، ساک، حرومزاده، بی‌ناموس، کس‌کش، مادرجنده، کیرم تو دهنت و...)
- بلاک ارسال پیام حاوی فحش و نمایش toast هشدار
- دکمه گزارش کنار هر پیام چت

## بخش ۷: جدول گزارش‌ها + سیستم اخطار/بن
- جدول `reports` با ستون‌های: `reporter_id`, `reported_user_id`, `type` (profile/chat), `reason`, `created_at`
- ستون‌های جدید در `profiles`: `warning_count`, `banned_until`, `is_permanently_banned`
- منطق خودکار:
  - ۳ گزارش معتبر → اخطار + نمایش بنر هشدار
  - ۶ گزارش → بن ۷ روزه
  - ۱۰ گزارش → بن دائمی
- نمایش متن قوانین این موارد در صفحه `/terms`
- جلوگیری از ورود کاربر بن شده

## بخش ۸: صفحه خرید بسته بازی
- مسیر جدید `/shop` با سه بسته: ۱۰۰ بازی / ۲۰۰ بازی / ۵۰۰ بازی
- دکمه پایین صفحه اصلی کنار پروفایل (آیکن کیف پول)
- اتصال به درگاه زرین‌پال یا نکست‌پی:
  - نیاز به Merchant ID از طرف شما (با ابزار `add_secret` می‌گیرم)
  - Server function برای ساخت تراکنش و گرفتن لینک پرداخت
  - Callback route برای تأیید پرداخت و اعتبار به `paid_games_remaining`
  - ذخیره تراکنش در `pi_payments` (تغییر نام به `payments`)

## ترتیب اجرا
1. اول مهاجرت‌های دیتابیس (یک‌جا)
2. حذف Pi و راه‌اندازی Auth جدید (نیاز به فعال‌سازی Google OAuth)
3. صفحه پرداخت (نیاز به Merchant ID — از شما می‌پرسم)
4. بقیه فیچرها به ترتیب

**سؤال قبل از شروع**: کدوم درگاه پرداخت؟ زرین‌پال یا نکست‌پی؟ (برای زرین‌پال فقط Merchant ID لازمه؛ نکست‌پی API Key)